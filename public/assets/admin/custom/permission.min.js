"use strict";

$(document).ready(function() {
	let $permissions_table = document.getElementById('permissions-table');
	if ( $permissions_table ) {
		$($permissions_table).DataTable({
			paging: true,
			lengthChange: true,
			searching: true,
			ordering: true,
			info: true,
			autoWidth: true,
			responsive: true,
    	});
	}
});